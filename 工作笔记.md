### 1. React生命周期

![img](https://p6-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/a7d8676f379d4d96bbf0ebd9a8528594~tplv-k3u1fbpfcp-zoom-in-crop-mark:1512:0:0:0.awebp)

挂载阶段： constructor、getDerivedStateFromProps、render、componentDidMount

更新阶段：getDerivedStateFromProps、shouldComponentUpdate、render、getSnapshotBeforeUpdate、componentDidUpdate

卸载阶段：componentWillUnmount

**constructor** :   （在React组件挂载之前调用，应在其他语句之前调用super）

+ 初始化state
+  绑定事件处理函数的 this

**getDerivedStateFromProps(nextProps, prevState)**：

+ 第一个参数是即将更新的props, 第二个参数是上一个状态的state，可以比较props和state来加一些限制条件，防止无用的state更新
+ 返回一个对象来更新state，如果返回null则不更新任何内容

**render:**

+ 它会读取当前的this.props和this.state，并利用这些数据构建出一个虚拟DOM树
+ 它返回的不是真实DOM节点，而是普通的js对象，描述了组件的结构
+ React会拿到这个新的虚拟DOM通过 **Diff 算法** 与上次虚拟DOM进行对比，找出最小差异，然后交给“提交阶段”去操作真实 DOM

**componentDidMount** :  在组件挂载后（插入DOM树后）立即调用，

+ 发送网络请求
+ 事件订阅
+ 启用事件监听

**shouldComponentUpdate(nextProps,nextState)** : 

+ 性能优化（根据更新前后的props或state来比较加一些限制条件，决定是否更新）

**getSnapshotBeforeUpdate(prevProps, prevState)** : 在render之后，即将对组件进行挂载时调用

+ 可以获取一些元素信息（例如滚动位置），这个生命周期返回的任何值都会作为参数传递给componentDidUpdate

**componentDidUpdate(prevProps, prevState, snapshot)**： 

`componentDidUpdate()` 会在更新后会被立即调用。首次渲染不会执行

**componentWillUnmount**: 

+ 取消网络请求
+ 移除监听事件
+ 清理DOM元素
+ 清理定时器

### 2.Websocket和SSE有什么区别？

从协议原理、通信模式、技术特性、应用场景思考

webSocket: 是一种基于TCP的全双工通信协议，在握手阶段借用了http协议，一旦连接建立，就会升级为独立的二进制协议

SSE: 是一种基于Http协议的单向推送技术。它利用Http的长连接特性，服务器通过 `Content-Type: text/event-stream`  持续向

客户端发送流式数据

| **特性**       | **WebSocket**                          | **SSE**                                       |
| -------------- | -------------------------------------- | --------------------------------------------- |
| **通信模式**   | **全双工** (双向同时传输)              | **单工/半双工** (服务器向客户端推送)          |
| **底层协议**   | WebSocket 协议 (基于 TCP)              | 标准 HTTP / HTTPS                             |
| **数据格式**   | 支持二进制 (Blob, ArrayBuffer) 和文本  | 仅限 UTF-8 文本                               |
| **连接维护**   | 需心跳包维持连接，断线需手动重连       | **内置自动重连**机制                          |
| **防火墙友好** | 需支持协议升级，有时会被防火墙拦截     | 纯 HTTP，**防火墙兼容性极佳**                 |
| **客户端上限** | 默认不受 HTTP 限制，但受服务器资源影响 | 浏览器对同域名长连接有限制 (HTTP/1.1 为 6 个) |

```javascript
// 客户端
const socket = new WebSocket('ws://example.com/chat');

socket.onopen = () => socket.send('Hello Server!');
socket.onmessage = (event) => console.log('收到消息:', event.data);

// 服务端 (ws库)
wss.on('connection', (ws) => {
  ws.on('message', (message) => {
    ws.send(`回显: ${message}`);
  });
});
```

```javascript
// 客户端
const eventSource = new EventSource('/stream');
eventSource.onmessage = (event) => {
  console.log('收到推送:', event.data);
};

// 服务端 (Express)
app.get('/stream', (req, res) => {
  res.setHeader('Content-Type', 'text/event-stream');
  res.setHeader('Cache-Control', 'no-cache');
  res.write('data: ' + JSON.stringify({ message: '当前时间' + new Date() }) + '\n\n');
});

// EventSource是浏览器原生提供的API
// 单向性： 它只允许服务端向客户端推送数据。如果客户端想发数据给服务端，需要另外发普通的http请求
// 自动重连：如果网络断开，浏览器会自动尝试重新连接，无需像WebSocket 那样手写重连逻辑

// SSE 本质上是一个“永不结束”的 HTTP 响应，关键在于这三个响应头：
// Content-Type: text/event-stream：告诉浏览器，这不是普通文件，而是一个持续的数据流。
// Cache-Control: no-cache：禁止缓存，确保推送的是实时数据。
// Connection: keep-alive：保持长连接。
```

为什么AI偏爱SSE？

+ 流式响应： LLM是逐token生成的，SSE天生支持流式传输，用户实时看到文字蹦出，体验极佳
+ 单向性足够：用户发送一个 Prompt（提问）后，主要是等待服务器长文本输出，不需要频繁的双向互动
+ 开发生成本低：SSE基于HTTP，不需要像Websocket那样去处理负载均衡的协议升级问题

什么时候用Websocket?

+ 实时协作：如多人编辑文档
+ 强交互游戏：需要极地延迟的双向指令传输
+ 二进制传输：需要在连接中频繁传输图片或者音频流

适合场景：

Websocket 适合重交互、高频双向、二进制。

SSE适合轻量级、服务端单向推送、流式文本

### 3.如果用户在旧页面，正好上线了新的版本出现报错怎样解决？

核心思想： 检测版本变化并强制刷新

1. 增量发布：部署时，**只增不减**

2. CDN回源（CDN容灾）：如果 CDN 找不到资源，配置它自动回源到备份服务器寻找

3. 版本文件轮询（主流做法）：打包时生成一个 `version.json`（存入当前时间戳或 Git Hash）。前端每隔一段时间（如 5 分钟）请求一次这个文件。对比本地版本号和服务器版本号，如果不一致，弹出提示框。

   ```javascript
   import { useEffect } from 'react';
   const useVersionCheck = () => {
     useEffect(() => {
       // 仅在生产环境运行
       if (process.env.NODE_ENV !== 'production') return;
   
       const checkVersion = async () => {
         try {
           const res = await fetch(`/version.json?t=${Date.now()}`);
           const data = await res.json();
           const localVersion = window.APP_VERSION; // 打包时注入的全局变量
   
           if (data.version !== localVersion) {
             // 这里可以弹出一个漂亮的 AntD Modal 提示用户
             console.log('发现新版本，建议刷新');
             // confirm('发现新版本，是否立即更新？') && window.location.reload();
           }
         } catch (e) {
           console.error('版本检查失败');
         }
       };
   
       // 每 5 分钟检查一次，或者在路由变化时检查
       const timer = setInterval(checkVersion, 1000 * 60 * 5);
       return () => clearInterval(timer);
     }, []);
   };
   ```

4. 资源加载捕获：使用   window.addEventListener('error', handleWindowError,true)监听资源加载失败(特别是js/css哈希变化引起的404)，捕获后主动弹出提示或自动刷新页面

5. 路由守卫刷新（有局限性的）：在SPA的路由钩子中检测版本，若检测到后端已更新，则执行window.location.reload()

6. 禁止缓存：在HTML的<head> 中添加Meta标签，确保页面不被本地缓存，每次请求都获取最新版本

   ```html
   <meta http-equiv="Expires" content="0"> //指定缓存过期的绝对时间。设置为 0 表示页面立即过期
   <meta http-equiv="Pragma" content="no-cache">  //用于向后兼容。no-cache 效果等同于告诉浏览器每次都去服务器校验。
   <meta http-equiv="Cache-control" content="no-cache"> //最核心的设置。 no-cache 表示必须先与服务器确认资源是否更改（协商缓存），而不是直接使用本地缓存
   ```

7. WebSocket/SSE 推送：后端发布完成后，通过长连接给所有在线客户发送“更新通知”

8. vite-plugin-pwa : 通过 vite-plugin-pwa提供的虚拟模块，轻松监听Service Worker的状态

   流程：

   + 构建时 **重新生成 Service Worker** ： 所有的js/css/assets都是hash文件名，pwa生成service-worker.js、workbox-*.js、precache manifest（资源清单）
   + 把 **构建 hash / 资源清单** 写进 SW
   + 导致 **SW 文件内容发生变化**
   + 浏览器检测到 SW 变了，走更新流程：定期（或刷新时）请求 `service-worker.js`，对比内容发现变化 → 触发 `install` 新 SW

   vite-plugin-pwa 本身不负责版本管理，它通过在每次构建时生成新的 Service Worker 文件，使其内容发生变化，从而触发浏览器原生的 Service Worker 更新流程。新 SW 安装后进入 waiting 状态，前端可通过 prompt 或 autoUpdate 策略让用户刷新页面，最终加载新的静态资源完成版本切换。

> install → waiting → activate
>
> 监听 SW 状态变化
>
> 当出现 `waiting` 状态
>
> 触发回调 / hook
>
> 你在 UI 上提示：发现新版本

> **小型/一般项目**：**“增量部署 + HTML 协商缓存 + Error Boundary 捕获”**。成本最低，能解决 99% 的 404 报错。
>
> **大型/核心业务**：在上述基础上增加 **“版本轮询提示”**。因为核心业务（如交易、管理后台）不仅要防奔溃，还要防接口不兼容，必须主动提醒用户刷新以获取最新的 API 调用逻辑。

```react
// Error Boundary解决方案
import React from 'react';

// 定义 UI 兜底组件（也可以单独写个文件）
const DefaultErrorFallback = ({ resetError }) => (
  <div style={{ padding: '40px', textAlign: 'center' }}>
    <h2>页面似乎出了一点问题</h2>
    <p>可能是因为我们发布了新版本，请尝试刷新页面。</p>
    <button 
      onClick={resetError}
      style={{ padding: '8px 16px', background: '#1890ff', color: '#fff', border: 'none', borderRadius: '4px', cursor: 'pointer' }}
    >
      手动刷新
    </button>
  </div>
);

class GlobalErrorBoundary extends React.Component {
  constructor(props) {
    super(props);
    this.state = { hasError: false, error: null };
  }

  // 渲染阶段捕获错误
  static getDerivedStateFromError(error) {
    return { hasError: true, error };
  }

  // 提交阶段处理副作用（自动刷新逻辑）
  componentDidCatch(error, errorInfo) {
    console.error("ErrorBoundary Caught:", error, errorInfo);

    // 核心逻辑：处理版本更新导致的资源加载失败
    const isChunkError = /Loading chunk \d+ failed/.test(error.message) || 
                         error.name === 'ChunkLoadError';

    if (isChunkError) {
      const lastReload = sessionStorage.getItem('last_version_reload');
      const now = Date.now();

      // 限制自动刷新频率：10秒内只允许自动刷新一次，防止死循环
      if (!lastReload || now - parseInt(lastReload) > 10000) {
        sessionStorage.setItem('last_version_reload', now.toString());
        window.location.reload(true);
      }
    }
  }

  resetError = () => {
    this.setState({ hasError: false, error: null });
    window.location.reload();
  };

  render() {
    if (this.state.hasError) {
      // 如果提供了自定义 fallback，则使用它，否则使用默认 UI
      return this.props.fallback || <DefaultErrorFallback resetError={this.resetError} />;
    }
    return this.props.children;
  }
}

// 导出高阶组件 (HOC)，方便包装 App
export function withGlobalErrorBoundary(Component, fallback) {
  return (props) => (
    <GlobalErrorBoundary fallback={fallback}>
      <Component {...props} />
    </GlobalErrorBoundary>
  );
}

export default GlobalErrorBoundary;
```

```react
// 在 React 入口文件（index.js/main.js）的最顶部执行
window.addEventListener('error', (event) => {
  // 检查是否是脚本加载错误
  const isScriptError = event.target && event.target.tagName === 'SCRIPT';
  
  if (isScriptError) {
    const url = event.target.src;
    console.warn(`资源加载失败: ${url}`);
    
    // 逻辑：如果是 chunk 加载失败，极大概率是版本更新
    if (url.includes('/static/js/')) {
       handleVersionUpdate();
    }
  }
}, true); // <--- 重点：必须为 true
```

```react
// 社区成熟的方案
import { ErrorBoundary } from 'react-error-boundary';

function ErrorFallback({ error, resetErrorBoundary }) {
  return (
    <div role="alert">
      <p>检测到新版本或程序异常:</p>
      <pre>{error.message}</pre>
      <button onClick={resetErrorBoundary}>尝试自愈</button>
    </div>
  );
}

const AppWrapper = () => (
  <ErrorBoundary
    FallbackComponent={ErrorFallback}
    onReset={() => {
      // 当点击重试时执行的逻辑
      window.location.reload();
    }}
    onError={(error) => {
      if (/chunk/i.test(error.message)) {
        window.location.reload();
      }
    }}
  >
    <App />
  </ErrorBoundary>
);
```

```react
//项目中使用vite-plugin-pwa

// 配置vite.config.js
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import { VitePWA } from 'vite-plugin-pwa';

export default defineConfig({
  plugins: [
    react(),
    VitePWA({
      registerType: 'prompt', // 关键：询问用户是否刷新
      devOptions: {
        enabled: true, // 开发环境下也开启 PWA，方便调试
      },
      workbox: {
        globPatterns: ['**/*.{js,css,html,ico,png,svg}'], // 缓存这些静态资源
      },
      manifest: {
        name: 'React PWA App',
        short_name: 'App',
        theme_color: '#ffffff',
        icons: [
          { src: 'pwa-192x192.png', sizes: '192x192', type: 'image/png' },
          { src: 'pwa-512x512.png', sizes: '512x512', type: 'image/png' },
        ],
      },
    }),
  ],
});

// 编写React更新提示组件
// src/components/ReloadPrompt.tsx
import React from 'react';
import { useRegisterSW } from 'virtual:pwa-register/react';

const ReloadPrompt: React.FC = () => {
  // 插件提供的 Hook
  const {
    offlineReady: [offlineReady, setOfflineReady],
    needRefresh: [needRefresh, setNeedRefresh],
    updateServiceWorker,
  } = useRegisterSW({
    onRegistered(r) {
      console.log('SW 注册成功');
      // 可以在这里设置定时检查更新 (例如每小时检查一次)
      r && setInterval(() => {
        r.update();
      }, 60 * 60 * 1000);
    },
    onRegisterError(error) {
      console.error('SW 注册失败', error);
    },
  });

  const close = () => {
    setOfflineReady(false);
    setNeedRefresh(false);
  };

  return (
    <div className="pwa-toast-container">
      {(offlineReady || needRefresh) && (
        <div className="pwa-toast">
          <div className="pwa-message">
            {offlineReady ? (
              <span>应用已准备好离线使用</span>
            ) : (
              <span>检测到新版本，请点击刷新以获取最新内容。</span>
            )}
          </div>
          <div className="pwa-buttons">
            {needRefresh && (
              <button className="pwa-button" onClick={() => updateServiceWorker(true)}>
                立即刷新
              </button>
            )}
            <button className="pwa-button" onClick={close}>
              稍后
            </button>
          </div>
        </div>
      )}
    </div>
  );
};

export default ReloadPrompt;

//在App.tsx中引用
// src/App.tsx
import ReloadPrompt from './components/ReloadPrompt';

function App() {
  return (
    <div className="App">
      <h1>我的 React 应用</h1>
      {/* 其他业务逻辑 */}
      
      {/* 放入更新提示组件 */}
      <ReloadPrompt />
    </div>
  );
}
```

#### 协商缓存:

第一对比：指纹对比 (ETag / If-None-Match) —— 最常用

+ **初次访问**：服务器返回 `index.html`，并在 Header 中带上 `ETag`（根据文件内容生成的唯一哈希值）
+ **再次访问**：浏览器带上 `If-None-Match: "上次的ETag"` 询问服务器。
+ 判断：如果文件没变，服务器返回 **304 Not Modified**。如果文件变了（发布了新版本），服务器返回 **200 OK** 和最新的 `index.html`

第二对：时间对比 (Last-Modified / If-Modified-Since)

+ 原理同上，只不过对比的是文件的**最后修改时间**。因为秒级精度和服务器时间偏差问题，目前 `ETag` 优先级更高。

| **特性**               | **window.onerror**                                       | **addEventListener('error')**      |
| ---------------------- | -------------------------------------------------------- | ---------------------------------- |
| **参数接收**           | 接收 5 个详细参数 (`msg`, `url`, `line`, `col`, `error`) | 接收一个 `ErrorEvent` 对象         |
| **绑定数量**           | 只能绑定一个（覆盖式）                                   | 可以绑定多个                       |
| **资源加载错误 (404)** | **无法捕获**                                             | **可以捕获** (需开启 `useCapture`) |
| **运行阶段**           | 冒泡                                                     | 冒泡或捕获（可选）                 |

#### 完整链路：

| **解决阶段** | **手段**                  | **效果**                                                                 |
| ------------ | ------------------------- | ------------------------------------------------------------------------ |
| **部署阶段** | **增量发布**              | 即使发布了新版，旧 JS 文件依然留在服务器/CDN，确保旧页面能加载到资源。   |
| **进入阶段** | **协商缓存**              | 确保新打开页面的用户拿到的是最新入口。                                   |
| **运行阶段** | **轮询/版本比对**         | 主动弹窗告诉用户：“系统已更新，请刷新”，防患于未然。                     |
| **崩溃阶段** | **Global Error Boundary** | 最后一道防线。如果 404 发生了，代码自动捕获并 `location.reload()` 救场。 |

### 4.高阶组件HOC

 HOC解决哪些问题：(HOC本质是组件装饰器)

+ **代码复用**： 当多个组件需要相同的逻辑时，不需要在每个组件里面重写一遍 （ 如：检查登录状态、数据埋点、订阅同一数据源）
+ **渲染劫持**：HOC可以控制包裹组件是否渲染，或者在渲染前后增加额外的UI。（例如：给所有表单项统一包裹一个带有错误提示的容器）
+ **Props操作**：HOC可以对传入的props进行转换、添加、删除，从而使原始组件更加“纯粹”，只负责展示逻辑

#### HOC的典型使用场景

+ 权限控制
+ 页面埋点与性能监控
+ 与第三方库的集成
+ 样式布局容器的封装

> 既然 HOC 解决了这么多问题，为什么 React 官方还要推行 Hooks 来取代它？
>
> 1. 嵌套地狱:  这是 HOC 最直观的缺点。当你需要给一个组件增加权限控制、日志记录、数据获取等多项功能时，你的组件树会变成这样： `withAuth(withLog(withData(withTheme(MyComponent)))) ` -----调试困难，性能有细微的影响
>
> 2.  Props 覆盖冲突：HOC 主要是通过注入 `props` 来传递数据。 如果 `withData` 注入了一个叫 `id` 的 prop，而 `withUser` 也注入了一个叫 `id` 的 prop，后者会直接覆盖前者，且编译器不会报错。这种“隐式依赖”导致开发者在编写业务组件时，很难溯源某个 `prop` 到底是从哪个 HOC 传进来的
>
> 3.  静态方法丢失与 Ref 失效： **静态方法丢失**：类组件上的静态方法不会自动挂载到 HOC 返回的新组件上，必须手动处理（如使用 `hoist-non-react-statics`）。
>
>    **Ref 转发**：默认情况下，`ref` 属性不能像 `props` 一样传递。如果你给 HOC 包装后的组件加 `ref`，它会指向 HOC 的实例，而不是内部的原始组件

| **维度**     | **高阶组件 (HOC)**                    | **Hooks**                      |
| ------------ | ------------------------------------- | ------------------------------ |
| **复用形态** | **组件组合**（Component Composition） | **函数调用**（Function Call）  |
| **层级结构** | 增加 DOM 节点/组件层级                | **扁平化**，不改变组件树       |
| **逻辑溯源** | 隐式注入，来源不透明                  | 显式调用，变量来源一目了然     |
| **适用场景** | 劫持渲染、统一容器包装、权限拦截      | 逻辑抽象、状态管理、副作用处理 |

### 5.函数柯里化 

**函数柯里化（Currying）**：把一个「接收多个参数的函数」， 转换成「**多次接收单个（或部分）参数的函数**」。

```js
function add(a, b, c) {
  return a + b + c
}

add(1, 2, 3)
// 柯里化：一次传完-->分多次传s
add(1)(2)(3) 


//手写一个最基本的柯里化函数
function curry(fn) {
  return function curried(...args) {
    if (args.length >= fn.length) {
      return fn.apply(this, args)
    } else {
      return function (...rest) {
        return curried.apply(this, args.concat(rest))
      }
    }
  }
}

// 使用示例
function add(a, b, c) {
  return a + b + c
}

const curriedAdd = curry(add)

curriedAdd(1)(2)(3)    // 6
curriedAdd(1, 2)(3)   // 6
curriedAdd(1)(2, 3)   // 6
```

柯里化的本质是：

- 利用 **闭包** 保存已经传入的参数
- 每次调用都判断参数是否收集够
- 不够就返回新函数，够了就执行原函数

### 6.React.memo的入参是什么？

React.memo(Component, arePropsEqual)

1. 第一个参数是React组件，

2. 第二个参数是比较函数，

   + **类型**：可选，一个接收两个参数的函数：`prevProps` 和 `nextProps`

   + **作用**：允许你**自定义**比较逻辑，决定组件是否需要更新

   + **返回值**：

     - 返回 `true`：表示 Props 相等，**不触发**重新渲染。
     - 返回 `false`：表示 Props 不相等，**触发**重新渲染。

     > 这与类组件中的 `shouldComponentUpdate` 的逻辑**正好相反**（后者返回 `true` 代表要更新）

如果不传第二个参数，`React.memo`会默认进行浅比较： 

+ **基本类型**（如 `string`, `number`）：值相等则不更新。
+ **引用类型**（如 `object`, `array`, `function`）：**比较内存地址**。

> 为什么我用了 `React.memo`，父组件更新时子组件还是重新渲染了？
>
>  因为父组件在每次渲染时可能重新创建了对象或函数（比如内联函数或 `style={{...}}`）。即使内容没变，但**引用地址变了**，`React.memo` 的默认浅比较会认为 Props 已改变。此时需要配合 `useCallback` 或 `useMemo` 来稳定引用地址

```react
const MyComponent = React.memo(
  (props) => {
    /* 渲染逻辑 */
  },
  (prevProps, nextProps) => {
    // 只有当 id 相等时，才不重新渲染
    return prevProps.id === nextProps.id;
  }
);
```

### 7.为什么 React 不默认给所有组件都加上 `React.memo`？

**缓存是有成本的**：`React.memo` 并不是免费的。每次父组件渲染时，它都要执行一次 `arePropsEqual` 函数来比较旧 Props 和新 Props。

**比较的开销 vs 渲染的开销**：

- 如果一个组件结构很简单（比如就几行 HTML），**执行对比算法的时间**可能比**直接重新渲染一遍**还要长。
- 如果组件的 Props 几乎每次都在变（比如传了个 `children` 或者频繁变动的时间），那么 `memo` 永远返回 `false`。结果你不仅白白执行了对比函数，最后还得重新渲染，这叫**双重浪费**

**结论**：只在组件渲染昂贵、且 Props 经常保持不变的情况下使用它。

### 8. `useCallback` 和 `useMemo` 的区别是什么？

> **useMemo 用来缓存“计算结果”，useCallback 用来缓存“函数本身”**
>  本质上：**useCallback 是 useMemo 的语法糖**
>
> 都接收两个参数：一个回调函数和一个依赖数组。
>
> useMemo: 回调函数用于计算，依赖项数组中只要有值发生变化时，才会重新计算返回新的值，否则返回上一次缓存的值。
>
> useCallback: 当依赖项发生变化时，会返回一个新的回调函数，否则会复用之前的回调函数。

useMemo: 避免重复执行“开销大的计算”

```react
const memoValue = useMemo(() => {
  return expensiveCalc(a, b)
}, [a, b])
//它缓存的是函数的返回值
```

适合场景：

+ 计算复杂（循环、排序、过滤大量数据）
+ 依赖不变时，不想重复计算

useCallback: 避免函数在每次 render 时重新创建

```react
const handleClick = useCallback(() => {
  console.log(count)
}, [count])
//它缓存的是函数引用本身
```

适合场景：

+  函数作为 props 传给子组件
+ 函数放在 `useEffect` / 依赖数组里
+ 搭配 `React.memo` 使用

```react
useCallback(fn, deps)
// 等价于
useMemo(() => fn, deps)
```

| **特性**     | **useMemo**                                              | **useCallback**                                         |
| ------------ | -------------------------------------------------------- | ------------------------------------------------------- |
| **缓存什么** | 缓存 **计算结果**（可以是对象、数组、值）                | 缓存 **函数本身**                                       |
| **核心目的** | 避免昂贵的计算在每次渲染时重复执行                       | 避免因为函数引用变化导致子组件 `memo` 失效              |
| **代码本质** | `const memoizedValue = useMemo(() => compute(), [deps])` | `const memoizedFn = useCallback(() => { ... }, [deps])` |

```react
const Parent = () => {
  const [count, setCount] = useState(0);

  // 坑点 1：每次渲染都会创建一个新的对象引用
  const userInfo = { name: 'Gemini', age: 1 };

  // 坑点 2：每次渲染都会创建一个新的函数引用
  const handleClick = () => console.log('clicked');

  return (
    <div>
      <Child data={userInfo} onClick={handleClick} />
      <button onClick={() => setCount(count + 1)}>更新父组件</button>
    </div>
  );
};

// 即使加了 memo，子组件依然会重新渲染！
const Child = React.memo(({ data, onClick }) => {
  console.log('子组件渲染了');
  return <div onClick={onClick}>{data.name}</div>;
});
```



“在这个案例中，`Child` 组件虽然加了 `memo`，但因为父组件渲染时 `userInfo` 对象和 `handleClick` 函数的**引用地址**发生了变化，导致 `memo` 的浅比较判断失效。

**修正方案：** 应该用 `useMemo` 包裹 `userInfo`，用 `useCallback` 包裹 `handleClick`。这样只要依赖项不变，引用地址就保持恒定，`memo` 就能真正起作用。”

总结：

**useMemo：缓存计算结果**

**useCallback：缓存函数引用**

**函数传 props → 用 useCallback**

**计算开销大 → 用 useMemo**

**不要为了“看起来专业”而滥用**

### 9. 手写一个函数防抖？

在一段时间内，多次触发只执行最后一次

```js
function debounce(fn, delay) {
  let timer = null

  return function (...args) {
    if (timer) {
      clearTimeout(timer)
    }

    timer = setTimeout(() => {
      fn.apply(this, args)
    }, delay)
  }
}
//timer 存在闭包里，用来记录定时器
//每次触发先清掉上一次
//只保留最后一次执行

//支持 cancel / flush
function debounce(fn, delay, immediate = false) {
  let timer = null

  function debounced(...args) {
    const context = this

    if (timer) clearTimeout(timer)

    if (immediate) {
      const callNow = !timer

      timer = setTimeout(() => {
        timer = null
      }, delay)

      if (callNow) fn.apply(context, args)
    } else {
      timer = setTimeout(() => {
        fn.apply(context, args)
      }, delay)
    }
  }

  debounced.cancel = function () {
    if (timer) {
      clearTimeout(timer)
      timer = null
    }
  }

  debounced.flush = function () {
    if (timer) {
      clearTimeout(timer)
      fn()
      timer = null
    }
  }

  return debounced
}
```

> 为什么要用 apply / this？
>
> 为了保证 debounce 后的函数
>
> - `this` 不丢失
> - 参数正常透传
>
> **为什么 timer 要放在函数外？**
>
> 因为要利用闭包，在多次调用之间共享状态。

总结：防抖的核心是闭包 + setTimeout
 多次触发只执行最后一次
 通过 clearTimeout 保证前一次失效
 通过 apply 保证 this 和参数不丢失
 高级版支持立即执行和 cancel

###  10.React中怎样实现父组件调用子组件的方法？

思路： 在子组件中，创建一个公开方法给父组件使用

**类组件**： 使用父组件中createRef创建一个ref对象，保存对子组件的引用

```react
class ChildComponent extends React.Component{
  childMethod(){
    // 子组件中需要执行的操作
	}
  render(){
    //子组件的渲染逻辑
  }
}

class Parentcomponent extends React.Component{
   constructor(props){
     super(props); 
     //super的作用：
     //1. 激活this：允许子组件使用this关键字（子类本身没有this，通过super调用父类父类（React.Component）的构造函数，从而继承父类的this对象）
     //2.属性传递：绑定props到this（React.Component 的构造函数会将接收到的 props 挂载到实例上）
     //3.继承逻辑	执行父类 React.Component 的初始化逻辑。
     this.childRef = React.createRef();
		}
  handleClick(){
    	// 调用子组件的方法
    this.childRef.current.childMethod()
	}
  render(){
    return (
    	<div>
        <ChildComponent ref={this.childRef} />
      </div>
    )
	}
}
```

函数式组件：子组件使用forwardRef包装，并利用useImperativeHandle定义哪些方法可以被父组件使用

```react
import React, { useImperativeHandle, forwardRef, useState } from 'react';

//子组件
const Child = forwardRef((props, ref) => {
  const [count, setCount] = useState(0);

  // 暴露给父组件的具体接口
  useImperativeHandle(ref, () => ({
    // 这里定义父组件能调用的方法
    increment() {
      setCount(prev => prev + 1);
    },
    reset() {
      setCount(0);
    }
  }));

  return <div>子组件计数器: {count}</div>;
});

export default Child;

//父组件 

import React, { useRef } from 'react';
import Child from './Child';

function Parent() {
  // 1. 创建 ref
  const childRef = useRef(null);

  const handleCallChild = () => {
    // 2. 通过 current 调用子组件暴露的方法
    if (childRef.current) {
      childRef.current.increment();
    }
  };

  return (
    <div>
      <button onClick={handleCallChild}>点击调用子组件方法</button>
      <Child ref={childRef} />
    </div>
  );
}
```

> 总结：
>
> forwardRef ： React中的`ref`是为了直接操作DOM节点，因为函数组件没有实例，forwardRef允许组件接收ref，并将其向下传递给子组件。
>
> useImperativeHandle: 它决定了父组件ref.current 最终拿到的内容。它起到了防火墙的作用，避免父组件直接操作子组件内部的复杂DOM和状态，只暴露必要的api。

在 React 开发中，请记住一个原则：**能用 Props 解决的，就不要用 Ref。**

- 如果你想让子组件变色，应该传一个 `color` prop。
- 如果你想让子组件里的输入框立刻获得焦点，这才是用 `ref` 的最佳时刻。

**ref实例的使用场景**： 

| **特性**               | **传递给 DOM**            | **传递给子组件**                             |
| ---------------------- | ------------------------- | -------------------------------------------- |
| **`ref.current` 指向** | 真实的 HTML DOM 节点      | 组件实例 或 `useImperativeHandle` 定义的对象 |
| **主要目的**           | 调用浏览器 API            | 调用组件内部定义的逻辑方法                   |
| **破坏性**             | 较低（属于正常 DOM 操作） | 较高（过度使用会导致组件间耦合严重）         |
| **推荐度**             | 常用                      | 慎用（优先考虑状态提升）                     |

### 11.你常用的React Hooks有哪些？

`useState`: 用于在函数组件中添加状态管理

`useEffect`: 用于处理副作用操作 (数据获取，订阅，事件监听等)

`useContext`: 用于在组件树中获取和使用共享上下文

`useReducer`: 用于管理复杂状态逻辑的替代方案，类似于redux和reducer

`useCallback`： 用于缓存回调函数，以便在依赖未变化时避免重复创建新的函数实例

`useMemo`: 用于缓存计算结果，以便在依赖未变化时避免重复计算

`useRef` : 用于在函数组件之间保存可变的值，并且不会引发重新渲染

`useLayoutEffect`： 与useEffect类似，但在浏览器完成绘制之前同步执行。

`useImperativeHandle` : 用于自定义暴露给父组件的实例值或方法

`useDebugValue`: 用于在开发者工具中显示自定义钩子相关标签

### 12.useEffect的第二个参数，传空数组和传依赖数组有什么区别？

useEffect用于处理组件生命周期中的副作用，useEffect接收两个参数：一个是要执行的函数，第二个是依赖数组

当传递空数组[]时：useEffect只会在组件挂载和卸载时调用一次，这种情况下，useEffect不会监听任何变量，不会对组件重新渲染

```react
useEffect(()=> {
	// 只在组件挂载和卸载时执行
},[])
```

当传递依赖数组时：useEffect会在组件挂载和依赖项更新时调用。当依赖项中的任何一个值发生变化时，useEffect都将被重新调用。

```react
useEffect(() => {
	//在组件挂载，卸载阶段以及任意依赖项发生变化时调用
},[dep1,dep2])
```

当依赖数组为空时，每次组件重新渲染时都会调用useEffect和要执行的函数

### 13. React Portals 有什么用？

React Portals是React提供的一种机制，用于将子组件渲染到父组件DOM结构层次之外的位置。且仍保留组件的React上下文（props、state、事件）它在处理一些特殊情况下的UI布局或交互时非常有用。

**核心特性：**

+ 渲染脱离父组件的DOM层级，解决**父组件样式限制**（如 `overflow: hidden`、`z-index` 层级不足）问题
+ 组件仍属于原React组件树，**事件冒泡、上下文传递完全正常**，区别于直接手动操作DOM
+ 常用于实现弹窗、提示框、悬浮层、侧边栏等脱离父容器的组件

**关键注意点：**

- 目标 DOM 节点需**提前在 HTML 中存在**，避免渲染时节点未定义。
- 服务端渲染（SSR）需额外处理，确保客户端 hydrate 时目标节点存在。
- 组件卸载时，React 会**自动清理**Portal 渲染的 DOM 节点，无需手动删除。

**使用场景：**

1. 在模态框中使用： 当你需要在应用的根DOM结构之外显示模态框（对话框）时，React Portal可以帮助你将模态框的内容渲染到根DOM之外的地方，而不影响布局。
2. 处理z-index问题：在一些复杂的布局中，可能存在z-index的层级关系导致组件无法按照预期的方式叠加显示。使用React Portals 可以将组件渲染到具有更高z-index的容器中，以解决这些问题。
3. 在全局位置显示组件：如果你希望某个组件在页面的固定位置显示，而不受父组件的定位影响，React Portal可以将该组件渲染到body或其他容器中。
4. 在动画中使用：当你需要在页面的某个位置执行动画时，React Portals可以帮助你动画的内容渲染到离组件更近的DOM结构中，以提高动画性能。

### 14. React和React DOM

react 和 react-dom 是React库的两个主要部分，他们分别负责处理不同的事务

+ **react**: 核心库，包含了React的核心功能， 如组件、状态、生命周期等。它提供了用户界面所需的基本构建块。
+ **react-dom**: 这是React专门为DOM环境提供的包，它包含了与浏览器DOM的相关功能。`react-dom`提供了用户在浏览器中渲染React组件的方法，包括ReactDOM.render。在Web开发中，`react-dom`被用于将React应用渲染到浏览器的DOM中

> 为什么要拆成React和React DOM?:  为了跨平台，react和react-dom是为了分离React的核心功能，以便更好的处理不同的环境和平台，这种分离使得React更加灵活，可以适应不同的渲染目标，而不仅仅局限于浏览器环境。React Native **不用 React DOM**，Web 才用 React DOM
>
> React 是平台无关的核心库，只负责描述 UI 和状态管理，React DOM 是 Web 端的渲染器，负责把虚拟 DOM 映射成真实 DOM。

### 15.react中怎样捕获异常？

> React 用 Error Boundary 捕获渲染阶段错误，用 try/catch 处理事件和异步异常，并通过全局监听做兜底上报。
>
> 👉 Error Boundary 只能捕获“同步渲染错误”，并不能捕获并发渲染中的异步逻辑错误。
>
> 👉 所以生产环境一定要 **Error Boundary + 全局异常监控**。

1.ErrorBoundary （不能使用函数式组件）

```react
异常来源
├── 渲染 / 生命周期 / 构造函数 → Error Boundary
├── 事件处理函数 → try / catch
├── 异步代码 (Promise / async) → try / catch + .catch
├── 全局兜底 → window.onerror / unhandledrejection
```

```react
class ErrorBoundary extends React.Component {
  state = { hasError: false }

  static getDerivedStateFromError(error) {
    return { hasError: true }
  }

  componentDidCatch(error, info) {
    // 上报日志
    console.error(error, info)
  }

  render() {
    if (this.state.hasError) {
      return <h1>页面出错了</h1>
    }
    return this.props.children
  }
}

// 使用

<ErrorBoundary>
  <MyComponent />
</ErrorBoundary>
```

2. 可以使用开源库：react-error-boundary， 上述有示例代码 

+ 支持reset (原生不方便 )
+ 支持key变化自动reset
+ 支持 Hook（函数组件专属）

```react
resetErrorBoundary()

<ErrorBoundary resetKeys={[userId]} />

import { useErrorBoundary } from 'react-error-boundary'
const { showBoundary } = useErrorBoundary()

try {
  throw new Error('手动抛错')
} catch (e) {
  showBoundary(e)
}

```



| 对比项       | 原生 Error Boundary | react-error-boundary             |
| ------------ | ------------------- | -------------------------------- |
| 官方支持     | ✅ React 官方        | ❌ 第三方库                       |
| 实现方式     | **必须 Class 组件** | **内部 Class，外部支持函数组件** |
| 函数组件可用 | ❌ 不可              | ✅ 可                             |
| 捕获错误范围 | render / 生命周期   | render / 生命周期                |
| 事件处理错误 | ❌ 不支持            | ❌ 不支持                         |
| 异步错误     | ❌ 不支持            | ❌ 不支持                         |
| fallback UI  | 手动 state 控制     | `fallback` / `FallbackComponent` |
| 错误回调     | `componentDidCatch` | `onError`                        |
| 错误重置     | 手动实现            | `resetErrorBoundary`             |
| reset 条件   | 不支持              | `resetKeys`                      |
| Hook 支持    | ❌                   | ✅ `useErrorBoundary`             |
| 上报监控     | 手动                | 更方便                           |
| 代码复杂度   | 偏高                | 低、语义清晰                     |

> 生产环境怎么选？react-error-boundary + 全局异常监控（Sentry / window.onerror）

try/catch:  可以捕获同步和async/await的异常

### 16. js中的原型链和原型（对象）介绍？

> 原型对象(普通的js对象)是构造函数为其实例提供的“共享属性和方法的载体”，所有的的实例通过内部的[[ProtoType]]中的\__proto__指向它
>
> 在 JavaScript 中，对象真正的原型关系存储在内部的 `[[Prototype]]` 槽中，`__proto__` 只是浏览器为了方便访问这个内部槽提供的一个访问器属性，它并不是语言层面的标准属性。从行为上看，它相当于一个指向原型对象的指针，但在规范中推荐使用 `Object.getPrototypeOf` 和 `Object.setPrototypeOf` 来操作原型关系。

+ 函数是特殊的对象，每一个函数对象有一个**prototype**属性，此属性指向自己的原型（对象）
+ 每一个函数的原型对象都有一个属性constructor**此属性指向自己所从属的函数本身**

```js
console.log(me.__proto__ === Person.prototype); // true (第5条：实例指向构造函数的原型)
console.log(Person.prototype.constructor === Person); // true (第8条：原型指回函数)
//在现代开发中，官方更推荐使用 `Object.getPrototypeOf(obj)` 来获取原型，因为 `__proto__` 在某些环境下可能被隐藏或性能较差。
```

**`prototype` (显式原型)**：**函数对象**才拥有该属性，它是一个指针，指向原型对象

**`[[Prototype]]` (隐式原型 - 内部属性)**：：**所有对象**都有这个内部属性，它是对象真正的原型链接。当访问一个对象不存在的属性时，JavaScript 引擎会沿着这条链向上查找

`__proto__` (隐式原型 - 访问器)：所有对象都有该属性，它是 `[[Prototype]]` 的非标准暴露接口（由浏览器实现，非 ECMAScript 核心标准），用于读取或设置对象的 `[[Prototype]]`



+ \__proto__是指向该对象的构造函数所对应的原型对象
+ **函数本身也是由 `Function` 构造出来的** ，`Function.__proto__ === Function.prototype`

+ 所有的原型对象最终都指向 Object：`Person.prototype` 是一个对象，它的 `__proto__` 指向 `Object.prototype`。而 `Object.prototype.__proto__` 最终指向 `null`，这是原型链的尽头。  `Object.prototype.__proto__ === null` // **true**

| **特性**   | **prototype**              | **\__proto__ / [[Prototype]]**           |
| ---------- | -------------------------- | ---------------------------------------- |
| **所属者** | 仅函数对象                 | 所有对象                                 |
| **性质**   | 标准属性                   | `__proto__` 为非标准（但通用）的访问属性 |
| **用途**   | 存储供实例继承的方法和属性 | 用于实现原型链的查找路径                 |
| **指向**   | 指向该函数的原型对象       | 指向创建该对象的构造函数的 `prototype`   |

### 17. script标签放在head里和body底部有什么区别？

script标签放在head部分：

+ 优点是预加载和全局可用性：浏览器渲染页面之前会下载解析head里面的所有脚本文件，确保页面加载过程随时被调用
+ 缺点是阻塞渲染，页面白屏时间延长：浏览器在遇到head里的script标签时会暂停HTML的解析和渲染，只到脚本下载并执行完毕，这可能导致页面加载变慢，白屏之间延长

script标签放在body底部：

+ 优点是非阻塞渲染和更好的用户体验：放在底部意味着浏览器会优先下载和渲染HTML内容，更快的看到内容然后再执行脚本，提高了页面的响应速度和用户体验

+ 缺点： 延长脚本执行，如果某些脚本需要页面加载之前运行（如某些初始化脚本），放在底部会影响功能

### 18 defer属性和async属性的区别？

默认情况（完全阻塞），当浏览器解析HTML到`<head>` 内部的` <script src="">` 会停止HTML解析，开始下载js文件，脚本下载完后立即执行脚本，等脚本执行完，浏览器才开始解析body的内容

`<head>` 部分使用`<script>` 标签时，可以增加defer属性：

+ **异步下载**：浏览器开启一个后台线程去下载js文件，同时html解析器照常工作，继续构建DOM树
+ **等待解析完成**：即使js下载好了，不会立即执行，而是等整个HTML文档解析完成（即生成完整的DOM树）
+ **按序执行**：浏览器会按照先后顺序，依次执行这些defer脚本

`async属性`  ：（异步并行）

+ **并行下载**：浏览器会开启一个后台线程去下载js文件，同时解析HTML
+ **插队执行**：一旦js下载完,浏览器会立即停止HTML解析，把js执行掉
+ **恢复解析**：js执行完后，继续解析剩下的HTML

### 19 Vite打包的流程

本质是基于Rollup构建的一套自动化构建流程，

graph TD
    A[执行 vite build] --> B[初始化配置]
    B --> C[解析入口文件（如index.html）]
    C --> D[预构建依赖（优化第三方包）]
    D --> E[模块转换（ESBuild编译）]
    E --> F[Rollup打包（代码分割/Chunk生成）]
    F --> G[代码优化（压缩/哈希/资源处理）]
    G --> H[输出产物到outDir]

+ **读取配置与环境变量**：Vite加载 vite.config.js 配置文件，根据当前环境模式加载对应的.env.produciton 文件，将对应环境变量注入到代码中
+ **依赖预构建**： 利用高性能的esbuild将第三方依赖（CommonJS/UMD）转换为ESModule格式，以确保在生产环境下能够正确解析。
+ **模块解析与依赖图生成**：Vite以根目录下的index.html为入口，Rollup解析该文件并开始分析依赖树，构建模块之间的依赖关系图。
+ **代码转换与插件处理**：遍历依赖图，应用各类插件（如Vue、React、CSS）将typescript、jsx、sass等非标准代码转化为标准、可优化的代码。
+ **打包与优化** ：
  + Tree Shaking： 移除未使用的代码和模块。
  + 代码分割（Code Splitting）：依据配置，将代码分割为多个chunk，以优化加载速度
+ **产物压缩与输出**： 将处理后的文件进行代码压缩js、css，生成最终产物，并将文件输出到预设的dist目录
+ **生成Manifest（可选）**：若配置了build.manifest，会生成manifest.json文件，用于映射源码与打包后的文件关系，常用于后端渲染环境



### 20. webpack 开发服务器的启动流程

+ 入口分析：从`entry`开始，递归解析所有依赖
+ Loader处理：对每个文件（ts,css,scss,vue,less）进行转换
+ 打包合并：将所有模块打包成一个或多个巨大的bundle文件
+ 启动服务：最后才将Bundle提供给浏览器

### 21. webpack打包的流程以及每个阶段做了什么（生命周期）

#### 初始化阶段：  

 这个阶段读取配置、准备环境

+ **读取参数**：从配置文件（webpack.config.js）和命令行（shell）中读取并合并参数
+ **实例化Compiler**：用上一步得到的参数初始化`Compiler`对象（这个对象是全局唯一的），代表了整个Webpack环境。
+ **加载插件**：执行配置文件中所有的plugins。 插件会监听Webpack广播出来的生命周期事件（Hooks），准备在特定的时间点干预构建过程

#### 编译阶段：

最耗时、最核心的阶段

+ **确定入口**：根据entry配置找到所有的入口文件

+ **编译模块**：从入口文件开始，调用对应的Loader对模块进行转译（比如把ts转成js）
+ **递归追踪**：如果该模块还依赖其他模块，则递归地进行编译
+ **生成AST**：Webpack会通过解析（Parser）生成抽象语法树（AST），以此来分析模块间的依赖关系
+ **构建依赖图**：最终在内存中形成一颗完整的依赖关系图

#### 输出阶段：

所有模块处理完后，Webpack开始准备交货

+ **组装Chunk**：根据入口和模块之间的依赖关系，将模块组合成一个个Chunk（代码块）
+ **生成Asset**:  把每个Chunk转换成一个单独的文件加入到输出列表 （这一步是最后修改输出内容的机会）
+ **写入文件系统(Emit)**：确定好输出内容后，根据output配置，把文件内容写入到硬盘（dist目录）

| **钩子名称**    | **触发时机**                          |
| --------------- | ------------------------------------- |
| **environment** | 环境准备就绪                          |
| **run**         | 开始编译                              |
| **compile**     | 即将创建 Compilation 对象             |
| **make**        | **最核心：** 从入口开始分析依赖并编译 |
| **seal**        | 封装 Chunk，禁止再修改模块            |
| **emit**        | **最常用：** 确定好数据，即将写入硬盘 |
| **done**        | 全部完成                              |

> Compiler vs Compilation
>
> Compiler：代表Webpack，从启动到停止的整个生命周期
>
> Compilation: 仅代表单次编译 （比如开发环境改了一行代码触发了HMR,就会创建一个新的Compilation）

```js
const HtmlWebpackPlugin = require("html-webpack-plugin");
const BundleAnalyzerPlugin =
  require("webpack-bundle-analyzer").BundleAnalyzerPlugin;
const MiniCssExtractPlugin = require("mini-css-extract-plugin");
const CssMinimizerPlugin = require("css-minimizer-webpack-plugin");
const path = require("path");

module.exports = (env, argv) => {
  const isProduction = argv.mode === 'production';

  return {
    // 入口文件
    entry: "./src/js/index.js",
    // 输出配置
    output: {
      filename: "[name].[contenthash].js", // 使用contenthash实现长期缓存
      path: path.resolve(__dirname, "dist"),
      clean: true, // 每次构建前清空输出目录
    },
    // 模块解析配置
    resolve: {
      alias: {
        "@": path.resolve(__dirname, "src"), // 路径别名
      },
      extensions: [".js", ".jsx", ".json"], // 自动解析文件扩展名
    },
    // 模块处理规则
    module: {
      rules: [
        {
          test: /\.css$/,
          // 根据环境选择CSS处理方式：开发环境内联，生产环境提取
          use: [
            isProduction ? MiniCssExtractPlugin.loader : "style-loader",
            "css-loader"
          ],
        },
        {
          test: /\.(png|svg|jpg|jpeg|gif)$/i,
          type: "asset", // webpack5内置资源处理
          parser: {
            dataUrlCondition: {
              maxSize: 8 * 1024, // 小于8kb转为base64
            },
          },
          generator: {
            filename: "assets/[name].[contenthash][ext]", // 资源输出路径和命名
          },
        },
        {
          test: /\.js$/,
          use: [
            {
              loader: "babel-loader",
              options: {
                presets: ["@babel/preset-env"], // ES6+转ES5
              },
            },
          ],
          exclude: /node_modules/, // 排除node_modules
        },
      ],
    },
    // 插件配置
    plugins: [
      new HtmlWebpackPlugin({
        filename: "index.html",
        template: "./src/index.html", // HTML模板
      }),
      // 生产环境专用插件
      ...(isProduction ? [
        new BundleAnalyzerPlugin({
          analyzerMode: "static", // 静态报告
          openAnalyzer: true, // 自动打开分析报告
        }),
        new MiniCssExtractPlugin({
          filename: "[name].[contenthash].css", // 提取CSS到单独文件
        }),
      ] : []),
    ],
    // 优化配置
    optimization: {
      minimizer: [
        // 生产环境启用CSS压缩
        ...(isProduction ? [
          new CssMinimizerPlugin(),
        ] : []),
      ],
      // 代码分割配置
      splitChunks: {
        chunks: 'all', // 对所有chunk进行分割
        cacheGroups: {
          // 第三方库分组
          vendor: {
            test: /[\\/]node_modules[\\/]/, // 匹配node_modules
            name: 'vendors',
            chunks: 'all',
            priority: 10, // 优先级最高
          },
          // 公共代码分组
          common: {
            name: 'common',
            minChunks: 2, // 至少被引用2次
            chunks: 'all',
            priority: 5, // 优先级次之
            reuseExistingChunk: true, // 复用已存在的chunk
          },
        },
      },
    },
    // 源码映射配置
    devtool: isProduction ? "source-map" : "eval-cheap-module-source-map",
  };
};

```



### 22. webpack和vite有哪些差异

1. 开发环境工作原理（核心差异）：

+ Vite： 利用现代浏览器支持原生`import`的特性，启动时不进行打包。直接启动开发服务器，当浏览器请求某个文件时，Vite才对该文件进行按需编译并返回。这使得Vite冷启动速度极快，不受项目规模的影响
+ Webpack： 启动时必须从入口文件开始优先分析全项目依赖，将所有模块提前打包成一个bundle.js文件，然后才能启动开发服务器。随着项目规模增大，启动和热更新（HMR）速度会明显变慢

2. 热更新（HMR）:

+ Vite: HMR是在原生ESM之上实现的，当文件修改时，Vite只需要让浏览器重新请求该单个模块，更新速度适中保持在毫秒级。
+ Webpack：当某个模块改变时，webpack需要重新计算依赖图并局部重新打包，随着项目变大，热更新会有明显的延迟感。

3. 生产环境构建：

+ Vite：使用Rollup打包，专注于生产环境的优化
+ Webpack：生产环境构建功能强大，兼容性极好

4. 生态与配置

| 打包工具       | Webpack                                | Vite                                                 |
| :------------- | :------------------------------------- | :--------------------------------------------------- |
| **生态成熟度** | **极高**，拥有海量的插件和 Loader      | **高**，生态增长迅速，且兼容大部分 Rollup 插件       |
| **配置复杂度** | **高**，配置项繁杂，学习曲线陡峭       | **低**，开箱即用，配置非常简洁                       |
| **应用场景**   | 适合大型、复杂、需要高度定制化的旧项目 | 适合现代前端项目（Vue3/React）、追求开发体验的新项目 |

5. 生产环境构建：

+ Webpack： 生产环境与开发环境逻辑一致，稳定性极强
+ Vite：为了保证生产环境代码的兼容性和tree-shaking效果，目前仍选择使用Rollup进行打包，这导致其开发环境和生产环境的构建行为并不完全一致

| 维度         | Rollup                           | Rolldown (Vite 8 默认)                     |
| :----------- | :------------------------------- | :----------------------------------------- |
| **底层语言** | JavaScript (运行在 Node.js 上)   | **Rust** (编译为机器码)                    |
| **执行性能** | 单线程限制，大项目构建较慢       | **原生多线程**，速度快 **10-30 倍**        |
| **功能定位** | 专注于库打包，功能精简           | 结合了 Rollup 的 API 与 **esbuild** 的功能 |
| **内置能力** | 需依赖大量插件 (如 TS, CommonJS) | **内置** TS/JSX 转换、Minify、CSS 打包等   |
| **开发商**   | 社区驱动 (Rich Harris 发起)      | **VoidZero 团队** (尤雨溪领衔)             |

> 假如引入一个lodash库，
>
> 在webpack中，当前运行npm run  dev ，Webpack会老老实实地去解析lodash里的几百个方法，把他们塞进内存里的那个大bundle里。进度条会卡在70%很久
>
> 在Vite中，Vite会进行依赖预构建，它发现你用了lodash，会直接调用esbuild，将其预处理为一个单独的ESM模块并缓存。当你刷新页面时，浏览器直接请求这个缓存好的文件，完全不影响主线程。
>
> 怎么选？
>
> 选择Vite： 
>
> 1. 追求极致的开发体验（启动快、更新快）
> 2. 现代浏览器应用（不再需要兼容老旧的 IE）
> 3. 新建的中小型项目。
>
> 选择Webpack：
>
> 1. 需要深度定制打包逻辑、极其复杂的构建流。
> 2. 需要兼容低版本浏览器（IE 11 等）
> 3. 已经处于维护后期的超大型老项目
> 

### 23.既然Vite开发环境这么快，为什么生产环境还要用Rollup打包，而不直接用build或者原生ESM部署呢？

1. 虽然 esbuild 速度极快（Go 语言编写），但它在处理**复杂的代码分割（Code Splitting）**和 **CSS 处理**方面，目前还不如 Rollup 成熟
   + Rollup的优势：Rollup是为了ESM而生的，他在Tree-shaking，代码混淆、以及跨模块的插件通信上极其精细
   + 生态兼容性：前端生态中有大量的 Rollup 插件，Vite 为了让用户能无缝使用这些插件，在生产端选择了完全兼容 Rollup 的架构
2. 如果在生产环境直接部署ESM(不打包，直接传几千个js文件，会遇到严重的性能问题) ，嵌套依赖产生的瀑布流请求, 这会导致数百个网络往返请求（Round Trips）,加载速度也远慢于单个经过优化的 Bundle

> Vite 并没有在生产环境强行追求 esbuild 的速度，而是选择了更稳、优化更好的 Rollup。这是典型的**工程化思维**——在开发阶段解决效率问题，在生产阶段解决质量与性能问题。

### 24.Vue项目Vite打包默认的配置文件

```ts
import { defineConfig, loadEnv } from 'vite'
import vue from '@vitejs/plugin-vue'
import AutoImport from 'unplugin-auto-import/vite'
import Components from 'unplugin-vue-components/vite'
import { ElementPlusResolver } from 'unplugin-vue-components/resolvers'
import ViteImagemin from 'vite-plugin-imagemin'
import viteCompression from 'vite-plugin-compression'
import { resolve } from 'path'

export default defineConfig(({ mode }) => {
  const isProduction = mode === 'production'

  return {
    plugins: [
      vue(),
      // 自动导入组件
      AutoImport({
        resolvers: [ElementPlusResolver()],
        imports: ['vue', 'vue-router', 'pinia'],
        dts: 'src/types/auto-imports.d.ts',
        eslintrc: {
          enabled: true,
          filepath: './.eslintrc-auto-import.json'
        }
      }),
      // 自动注册组件
      Components({
        resolvers: [ElementPlusResolver()],
        dts: 'src/types/components.d.ts',
        dirs: ['src/components']
      }),
      isProduction &&
        viteCompression({
          verbose: true,
          disable: false,
          threshold: 10240,
          algorithm: 'gzip',
          ext: '.gz'
        }),
      // 配置图片压缩
      isProduction &&
        ViteImagemin({
          gifsicle: {
            optimizationLevel: 7,
            interlaced: false
          },
          optipng: {
            optimizationLevel: 5
          },
          mozjpeg: {
            quality: 80
          },
          pngquant: {
            quality: [0.8, 0.9],
            speed: 4
          },
          svgo: {
            plugins: [{ name: 'removeViewBox' }, { name: 'removeEmptyAttrs', active: false }]
          }
        })
    ],
    resolve: {
      alias: {
        '@': resolve(__dirname, 'src'),
        '@components': resolve(__dirname, 'src/components'),
        '@views': resolve(__dirname, 'src/views'),
        '@utils': resolve(__dirname, 'src/utils'),
        '@styles': resolve(__dirname, 'src/styles')
      }
    },
    css: {
      preprocessorOptions: {
        scss: {
          additionalData: `@use "@/styles/element.scss" as *;`
        }
      }
    },
    server: {
      port: 3000,
      open: true,
      cors: true,
      proxy: {
        '/api': {
          target: 'http://localhost:8080',
          changeOrigin: true,
          rewrite: (path) => path.replace(/^\/api\//, '')
        }
      }
    },
    build: {
      target: 'es2015', // 目标浏览器
      outDir: 'dist', // 输出目录
      assetsDir: 'assets', // 静态资源输出目录
      assetsInlineLimit: 4096, // 小于 4KB 的资源内联为 base64
      sourcemap: false, // 不生成 sourcemap
      reportCompressedSize: false, // 不显示 gzip 压缩大小报告
      cssCodeSplit: true, // 启用 CSS 代码分割
      rolldownOptions: {
        output: {
          // 代码分块配置
          codeSplitting: {
            groups: [
              {
                name: 'vendor-vue',
                test: /node_modules[\\/](vue|pinia|vue-router)/,
                priority: 40
              },
              {
                name: 'vendor-element-plus',
                test: /node_modules[\\/]element-plus/,
                priority: 30
              },
              {
                name: 'vendor-axios',
                test: /node_modules[\\/]axios/,
                priority: 20
              },
              {
                name: 'vendor-other',
                test: /node_modules/,
                priority: 10
              }
            ]
          },
          chunkFileNames: 'assets/js/[name]-[hash].js', // 代码分块/异步加载文件
          entryFileNames: 'assets/js/[name]-[hash].js', // 入口文件
          assetFileNames: 'assets/[ext]/[name]-[hash].[ext]' // 静态资源输出路径
        }
      }
    }
  }
})

```

### 25. Vite打包生命周期分析

vite 打包的流程本质上是Rollup生命周期的超集

#### 1.配置读取与插件容器初始化

+ **解析配置**：合并vite.config.js、命令行参数和.env环境变量
+ **创建插件容器**：Vite实现了一个模拟Rollup的插件容器。可以直接使用Rollup插件
+ **调用configResolved钩子**： 插件修改最终配置的最后机会

#### 2.依赖预构建（可选）

虽然构建阶段主要靠Rollup，但Vite有时会在生产环境前再次确认依赖。不过，主要的预构建发生在开发阶段

。在生产打包时，Vite会确保node_modules中的CommonJS依赖被转换为ESM，以便Rollup处理

#### 3.资源解析与转译

（核心阶段，对应Rollup的buildStart、resolved、load 和 transform）

+ **解析**(Resolve)： 根据alias或相对路径找到文件真实位置
+ **加载**（Load）：读取文件内容
+ **转译**（Transform）：
  + TypeScript/JSX：通过esbuild进行极速转译（这是Vite快的核心，esbuild比Babel快几十倍）
  + CSS：处理PostCSS、CSSModules或Sass
  + 静态资源：将小图片转为Base64，大资源记录路径

#### 4. Rollup捆绑与输出 

当所有源码都被转译为标准ESM后，进入Rollup的标准打包流程：

+ **Tree Shaking** ： 移除未引用的冗余代码
+ **Chunking**： 根据`manualChunks` 配置将代码拆分为不同的JS文件
+ **优化(Minify)**: 使用Terser或esbuild压缩代码、混淆变量名。
+ **写入文件（Write）**: 将生成的bundle写入dist目录

| **阶段**     | **钩子名称**      | **用途**                                                        |
| ------------ | ----------------- | --------------------------------------------------------------- |
| **启动阶段** | `config`          | 修改 Vite 配置                                                  |
|              | `configResolved`  | 获取最终生成的配置，只读（建议只读，对象没有被Object.freeze()） |
|              | `configureServer` | 用于配置开发服务器（仅限 Dev 模式）                             |
| **解析阶段** | `resolveId`       | 自定义如何寻找模块路径                                          |
|              | `load`            | 自定义如何读取模块内容                                          |
| **转换阶段** | `transform`       | **最常用**，对代码内容进行修改（如混淆、注入代码）              |
| **输出阶段** | `generateBundle`  | 在写入文件前，修改产物或增加额外文件                            |
|              | `closeBundle`     | 打包结束，可用于清理工作或发送通知                              |

### 26 npm i 、 npm ci 、npm install 的区别？

npm i 是 npm install的简写

兼容性与卸载：用npm i安装的某些包，在旧版本npm中可能无法用npm uninstall血癌，需要用npm uninstall i
版本检测：npm i 会自动检测并尝试安装与当前node.js版本最兼容的包版本，在依赖复杂时更智能
报错处理： npm install 在遇到网络问题或者版本冲突时，通常会生成更详细的debug.log，调试更容易

npm ci 强制依赖 package-lock.json

| 特性           | npm install (i)                   | npm ci                       |
| :------------- | :-------------------------------- | :--------------------------- |
| **主要依赖**   | `package.json`                    | `package-lock.json`          |
| **锁定版本**   | 不一定会锁定（取决于 `^` 或 `~`） | 必须严格锁定                 |
| **修改锁文件** | 会更新 `package-lock.json`        | 绝不更新 `package-lock.json` |
| **节点模块**   | 增量更新 `node_modules`           | 删除后重建 `node_modules`    |
| **执行速度**   | 较慢                              | 较快                         |
| **最佳场景**   | 本地开发                          | CI/CD、生产环境              |

> 日常开发推荐使用npm i 但是在CI/CD环境或遇到奇怪的安装问题，使用npm install 或npm ci